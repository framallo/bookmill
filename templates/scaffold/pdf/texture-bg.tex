% Paper-texture page background for the retail/share PDF.
% Included via pandoc --include-in-header for retail PDFs. By default this is a
% NO-OP (plain page) so a freshly scaffolded project builds with no image asset.
% To add a textured background, drop a `paper-texture.jpg` into images/ and
% uncomment the block below.
%
% \usepackage{eso-pic}
% \usepackage{graphicx}
% \graphicspath{{images/}{./}}
% \AddToShipoutPictureBG{%
%   \AtPageLowerLeft{%
%     \includegraphics[width=\paperwidth,height=\paperheight]{paper-texture.jpg}%
%   }%
% }
